/**
 * @author kalladanr
 *create table INV_Role (
 *role_id bigint not null auto_increment,
 *rolename varchar(255),  primary key (role_id) ) creates tables in db with autoincr if used @GeneratedValue 

 */
package com.inv.model;
